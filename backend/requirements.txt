fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-multipart>=0.0.9

# Heatmap (Grad-CAM) support — see app/heatmap.py and model/README.md for
# how this was validated before being wired in here. CPU-only is fine.
torch>=2.4
transformers>=4.44
grad-cam>=1.5
opencv-python-headless>=4.10
numpy>=1.26
Pillow>=10.4
